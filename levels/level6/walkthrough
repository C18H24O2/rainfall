In this level, we oveflow the malloc'ed buffer to change the value of
the function pointer below, we put the address of the n() function
40 times to fill the buffer and edit the pointer, it is then executed,
giving us the pass
